using UnityEngine;

public class DeleteLater : MonoBehaviour
{
    // just to disable an annoying error,
    // because asset animation had event handlers and I cant be asked to remove all of them from every animation
    public void AnimationEventHandler() { } 
}
